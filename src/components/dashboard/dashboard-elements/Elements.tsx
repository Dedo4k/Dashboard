import ElementDescription from "./ElementDescription";
import TestElement from "./TestElement";

const elements = [
    new ElementDescription(TestElement, "Test element", "Element description")
];

export default elements;